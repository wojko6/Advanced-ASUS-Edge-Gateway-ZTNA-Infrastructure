# Engineering worklog — 2026-09-23

## Summary

Eight repository commits hardened router backup coverage, startup and firewall locking, and installer rollback-snapshot retention. The resulting `de1cf10` tree was staged and deployed to the reference ASUS TUF-AX5400 as `v2.1.4-dev` during controlled maintenance. A verified off-router backup and successful restore dry-run preceded the change; the post-apply router health check reported zero failures and warnings. The same day's later firmware upgrade to `3004.388.11_1-gnuton1_tuf` passed the project health check and IPv4 DNS/exit-node smoke tests. A post-upgrade UDP/1900 listener owned by `hostapd` disappeared after WPS was disabled; the dedicated USB exposure audit then passed.

## Repository changes

The following commits were recorded on `main` on 2026-09-23 (local time):

| Commit | Change |
| --- | --- |
| `9f4c008` | Include the active `wan-event` hook in router project backups. |
| `590fbdc`, `9159c7d` | Detect and reclaim stale `services-start` locks using a recorded process identity, while preserving the live lock owner. Add a regression test and CI coverage. |
| `dc78af7` | Make `flock` an explicit prerequisite before the managed firewall script changes rules; expose the dependency in the health check. |
| `1ab8383`, `4bc5c90`, `00da2ea`, `de1cf10` | Bound installer snapshot retention on JFFS, allocate unique timestamp-plus-counter snapshot names, recognize older snapshot names when pruning, and abort before live changes if retention fails. Extend installer/static tests and operations documentation. |

The workstation fast-forwarded from `00da2ea` to `de1cf10` before the maintenance run. The repository worktree was clean at that point.

The [GitHub Actions validation suite for `de1cf10`](https://github.com/wojko6/Advanced-ASUS-Edge-Gateway-ZTNA-Infrastructure/actions/runs/35834852522) completed successfully.

## Pre-change recovery checkpoint

- The router created `/opt/backups/asus-edge/asus-edge-20260923-100428.tar.gz` and its `.sha256` sidecar.
- The initial `scp` transfer failed because the router lacks the configured SFTP server. Repeating the transfer with `scp -O` copied both files to the Fedora workstation under `$HOME/Backups/ASUS-Edge/2026-09-23-prechange/`.
- The off-router archive passed the sidecar SHA-256 check. `scripts/restore.sh` accepted the copied archive in `--dry-run` mode with exit status `0`, checking its structure and internal payload hashes without restoring live files.
- Archive inspection identified 23 configuration/script files under `jffs` and `opt`, including `asus-edge.conf`, the three active project hooks, dnsmasq and Unbound configuration, managed scripts, and preserved legacy hooks.
- The private `config/edge.conf` needed by the installer was extracted from this verified backup into the workstation checkout; it remains excluded from Git.

This project backup is scoped to the files selected by `scripts/backup.sh`. It is not a full router image and intentionally excludes Tailscale state and authentication material. No restore `--apply` was performed.

## Pre-install checks and staged deployment

- Shell syntax checking of the private configuration succeeded (`CONFIG_OK`). `sh tests/test-static.sh` reported `Static tests passed.`
- The live router health check reported `Summary: 0 failure(s), 0 warning(s)` before deployment. Its checks included Entware/SSD availability, swap, Tailscale, WAN exit-node runtime prerequisites, managed IPv4/IPv6 chains, LAN DoT and classic-DNS policy, Unbound/DNSSEC, and syslog-ng.
- A small copy of release `de1cf10` plus the private configuration was transferred over SSH into a restricted `/tmp/asus-edge-de1cf10` directory. Syntax and presence checks returned `STAGE_RC=0`.
- The installer ran first without `--apply`, after confirming that the staged configuration matched the router's current configuration. It saved snapshot `/jffs/addons/asus-edge/backups/install-20260923-101459-0`, installed the managed files and hooks, and returned `INSTALL_STAGE_RC=0`. The active hooks were marked as project managed; the subsequent health check again reported zero failures and warnings.
- From the LAN, `scripts/install.sh --apply` saved a second snapshot, `/jffs/addons/asus-edge/backups/install-20260923-101603-0`, and applied the managed firewall policy. It returned `APPLY_RC=0`. The post-apply health check reported `Summary: 0 failure(s), 0 warning(s)`.
- The installer reported that preserved legacy hooks remained disabled by default. There is no recorded change to that configuration setting during this deployment.

## Post-change traffic checks

- A Fedora LAN query addressed directly to the router returned `NOERROR` and increased the UDP `RETURN` counter in `EDGE_LAN_DNS_PREROUTING` from `0` to `1`.
- A Fedora LAN query addressed to an external classic DNS server also returned `NOERROR` and increased the UDP `REDIRECT` counter from `0` to `1`. This supports the configured LAN UDP/53 redirect for the tested client. The `dig` `SERVER` line displayed the requested destination and does not independently identify the resolver that answered after redirect.
- The operator reported that Internet access worked on the Android phone using the ASUS as a Tailscale exit node over cellular data. A later post-firmware public IPv4 comparison matched the router's public IPv4. A client-side packet trace and an isolated test of management access from another tailnet role were not recorded. This is an IPv4 functional smoke test, not a full security-matrix validation.

## State and next validation

`v2.1.4-dev` was deployed with the `de1cf10` source staged on the router. The pre-change off-router archive and the two installer snapshots provide the recorded recovery points. Static checks, live router health, LAN classic-DNS UDP redirection, and operator-reported cellular exit-node browsing passed within their stated scopes.

Before expanding the deployment claims, repeat the role-specific remote-client access tests and capture sanitized evidence if it is needed for publication.

A read-only check of the [GNUton release list](https://github.com/gnuton/asuswrt-merlin.ng/releases/tag/3004.388.11_1-gnuton1) initially identified `3004.388.11_1-gnuton1` as the firmware candidate. The later selection and flash are recorded below. Later storage and telemetry features were not deployed during this session.

## Firmware upgrade and recovery preparation

- The router reported `productid=TUF-AX5400` and old firmware `3.0.0.4 388.9 2-gnuton1`; its `hw_version` NVRAM value was empty. The operator selected the GNUton TUF-interface image for this existing TUF-AX5400 deployment.
- A sanitized pre-firmware diagnostic snapshot was created on the router, copied to private workstation storage under `$HOME/Backups/ASUS-Edge/2026-09-23-prefirmware/`, and verified with its `SHA256SUMS`. Its content was not published to the repository.
- Separate private, encrypted and checksum-verified workstation backups covered exported router `.CFG` settings, the full JFFS filesystem, 3,665 NVRAM entries, and Tailscale state on `/opt`. The NVRAM dump is recovery reference material, not a command to restore raw NVRAM settings automatically.
- The old `3004.388.9_2-gnuton1` firmware image was kept as a rollback candidate. Its verified SHA-256 was `59b0c7f6818ccfa5f94f39090951ecb4df1580567fcdb7b838c5c259e7603394`.
- The chosen `TUF-AX5400_3004_388.11_1-gnuton1_tuf_puresqubi.w` image was verified against its release digest, SHA-256 `69a0458d7edb54a42e083029d3cb2c689c5fb9dce0339d0f245cf3b1398598a7`.
- Immediately before the flash, the router storage/swap preflight and the project health check passed; the health check reported zero failures and warnings.

## Post-firmware checks and WPS resolution

- The operator uploaded the verified TUF-interface image through the router's firmware upgrade page. After the reboot, NVRAM reported build `388.11` and extension `1-gnuton1_tuf`.
- The project health check again reported `Summary: 0 failure(s), 0 warning(s)` with `POST_UPGRADE_RC=0`. The ad-blocker statistics were visible in the router interface.
- A final project health check after disabling WPS also reported `Summary: 0 failure(s), 0 warning(s)` and `FINAL_HEALTH_RC=0`.
- From the Fedora LAN client, DNS queries to the router and to an external UDP/53 destination returned `NOERROR`. The managed DNS chain's router-destination UDP `RETURN` counter increased `128` to `129`; the UDP `REDIRECT` counter increased `0` to `1`. The requested external server address displayed by `dig` alone does not identify the resolver after redirection.
- With the phone on cellular data and connected through the ASUS as a Tailscale exit node, its observed public IPv4 matched the router's public IPv4.
- In a separate WAN management check, the phone used cellular data with Wi-Fi and Tailscale off. Connecting to the router's WAN IPv4 on HTTPS port `8443` returned `ERR_CONNECTION_TIMED_OUT`. A workstation comparison confirmed that the tested router WAN IPv4 matched the externally observed public IPv4. This demonstrates that this client could not reach WAN management over that IPv4/port at the time of the test; it does not test all networks or ports. The router's DDNS hostname separately produced `DNS_PROBE_POSSIBLE` with Tailscale off and on, so DDNS resolution needs its own follow-up and was not used to infer WAN filtering.
- Read-only DDNS follow-up found the DDNS client enabled, a configured hostname, and a public DNS `A` response (`NOERROR`) from `1.1.1.1`. The phone's browser lookup error remains unexplained; the public record alone does not validate DNS resolution on that phone.
- An initial test from the Fedora Tailscale client to the router's Tailscale IPv4 on TCP/8443 did not reach HTTPS, while the router GUI was reachable over LAN. The phone, using cellular data and Tailscale, initially showed `ERR_CONNECTION_ABORTED`. The router had one managed HTTPS `ACCEPT` rule and one HTTPS DNAT rule; neither client's current Tailscale IPv4 matched that exact admin `/32` rule. The later authorized policy change is recorded below.
- Both `/tmp/mnt/ENTWARE` and `/tmp/mnt/ROUTER_DATA` were mounted, swap was active, and `/jffs` showed 29% usage; the post-upgrade storage/swap check passed.
- The dedicated USB exposure audit initially reported `1 failure(s), 0 warning(s)`: a listener on port 1900 or 8200. Follow-up showed `dms_enable=0`, no `minidlna` process, no listener on 8200, and one UDP/1900 socket owned by `hostapd` (`0.0.0.0:1900`). `upnp_enable=1` was also observed; the router's UPnP/NAT setting was not changed in this step. WPS was enabled globally and on both Wi-Fi bands. After disabling WPS through the router interface, `wps_enable=0`, no listener remained on 1900 or 8200, and the USB exposure audit passed with `0 failure(s), 0 warning(s)` and exit `0`. This before/after check supports WPS as the source of the observed `hostapd` listener. Persistence after another reboot was not tested.
- Because the combined post-upgrade command used `set -e`, the initial audit failure prevented the first evidence collection attempt. A separate run successfully created `/tmp/asus-edge-evidence-20260923T092924Z`. Its five content files passed `sha256sum -c SHA256SUMS` after transfer with `scp -O` to private workstation storage under `$HOME/Backups/ASUS-Edge/2026-09-23-postfirmware/`. No raw snapshot was published. The snapshot predates the WPS change and therefore records the earlier state; the post-WPS audit result was checked separately.

## Tailnet administration policy update

- The operator selected the Fedora workstation and Android phone as the two authorized Tailscale administration clients. The workstation's ignored `config/edge.conf` matched the router's live private configuration before editing.
- Before the change, `/opt/backups/asus-edge/asus-edge-20260923-121847.tar.gz` was copied with `scp -O` to private workstation storage under `$HOME/Backups/ASUS-Edge/2026-09-23-admin-prechange/`. Its sidecar SHA-256 and the restore script's internal archive dry-run both passed. A separate copy of the original private `edge.conf` was saved in that directory.
- The private configuration had two consecutive definitions of `EDGE_ADMIN_TS_SOURCES`. They were replaced by one definition containing exactly the current Fedora and phone Tailscale IPv4 addresses as `/32` sources. The values remain private and excluded from Git; the other project settings were not intentionally changed. Shell syntax and the workstation static tests passed.
- The release files and private configuration were staged in a restricted router directory under `/tmp`; the staged configuration matched the workstation copy. From a LAN SSH session, the installer ran with `--apply` after checking that the live configuration still matched the original. It created an installer rollback snapshot and applied the managed firewall policy. The command checked exact HTTPS input and DNAT rules for both configured sources, then the router health check reported `Summary: 0 failure(s), 0 warning(s)` and the command ended with `ADMIN_APPLY_AND_HEALTHCHECK_OK`.
- The operator subsequently reported that the router administration page loads using the tailnet address. The browser marks HTTPS red; the precise certificate error and certificate identity have not yet been checked. The suggested Fedora `curl` probe used `--insecure`, and its status code was not recorded here. This result establishes reported page reachability, not validated TLS identity or confirmed login from each client. The router's temporary staging copy still needs removal after the connectivity checks.

Keep the recovery archives, firmware files and raw evidence in private storage. Do not publish router settings, device identifiers, authentication state, or unreviewed snapshots.

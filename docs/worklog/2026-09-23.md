# Engineering worklog — 2026-09-23

## Summary

Eight repository commits hardened router backup coverage, startup and firewall locking, and installer rollback-snapshot retention. The resulting `de1cf10` tree was staged and deployed to the reference ASUS TUF-AX5400 as `v2.1.4-dev` during controlled maintenance. A verified off-router backup and successful restore dry-run preceded the change; the post-apply router health check reported zero failures and warnings.

## Repository changes

The following commits were recorded on `main` on 2026-09-23 (local time):

| Commit | Change |
| --- | --- |
| `9f4c008` | Include the active `wan-event` hook in router project backups. |
| `590fbdc`, `9159c7d` | Detect and reclaim stale `services-start` locks using a recorded process identity, while preserving the live lock owner. Add a regression test and CI coverage. |
| `dc78af7` | Make `flock` an explicit prerequisite before the managed firewall script changes rules; expose the dependency in the health check. |
| `1ab8383`, `4bc5c90`, `00da2ea`, `de1cf10` | Bound installer snapshot retention on JFFS, allocate unique timestamp-plus-counter snapshot names, recognize older snapshot names when pruning, and abort before live changes if retention fails. Extend installer/static tests and operations documentation. |

The workstation fast-forwarded from `00da2ea` to `de1cf10` before the maintenance run. The repository worktree was clean at that point.

The [GitHub Actions validation suite for `de1cf10`](https://github.com/wojko6/Advanced-ASUS-Edge-Gateway-ZTNA-Infrastructure/actions/runs/35834852522) completed successfully.

## Pre-change recovery checkpoint

- The router created `/opt/backups/asus-edge/asus-edge-20260923-100428.tar.gz` and its `.sha256` sidecar.
- The initial `scp` transfer failed because the router lacks the configured SFTP server. Repeating the transfer with `scp -O` copied both files to the Fedora workstation under `$HOME/Backups/ASUS-Edge/2026-09-23-prechange/`.
- The off-router archive passed the sidecar SHA-256 check. `scripts/restore.sh` accepted the copied archive in `--dry-run` mode with exit status `0`, checking its structure and internal payload hashes without restoring live files.
- Archive inspection identified 23 configuration/script files under `jffs` and `opt`, including `asus-edge.conf`, the three active project hooks, dnsmasq and Unbound configuration, managed scripts, and preserved legacy hooks.
- The private `config/edge.conf` needed by the installer was extracted from this verified backup into the workstation checkout; it remains excluded from Git.

This project backup is scoped to the files selected by `scripts/backup.sh`. It is not a full router image and intentionally excludes Tailscale state and authentication material. No restore `--apply` was performed.

## Pre-install checks and staged deployment

- Shell syntax checking of the private configuration succeeded (`CONFIG_OK`). `sh tests/test-static.sh` reported `Static tests passed.`
- The live router health check reported `Summary: 0 failure(s), 0 warning(s)` before deployment. Its checks included Entware/SSD availability, swap, Tailscale, WAN exit-node runtime prerequisites, managed IPv4/IPv6 chains, LAN DoT and classic-DNS policy, Unbound/DNSSEC, and syslog-ng.
- A small copy of release `de1cf10` plus the private configuration was transferred over SSH into a restricted `/tmp/asus-edge-de1cf10` directory. Syntax and presence checks returned `STAGE_RC=0`.
- The installer ran first without `--apply`, after confirming that the staged configuration matched the router's current configuration. It saved snapshot `/jffs/addons/asus-edge/backups/install-20260923-101459-0`, installed the managed files and hooks, and returned `INSTALL_STAGE_RC=0`. The active hooks were marked as project managed; the subsequent health check again reported zero failures and warnings.
- From the LAN, `scripts/install.sh --apply` saved a second snapshot, `/jffs/addons/asus-edge/backups/install-20260923-101603-0`, and applied the managed firewall policy. It returned `APPLY_RC=0`. The post-apply health check reported `Summary: 0 failure(s), 0 warning(s)`.
- The installer reported that preserved legacy hooks remained disabled by default. There is no recorded change to that configuration setting during this deployment.

## Post-change traffic checks

- A Fedora LAN query addressed directly to the router returned `NOERROR` and increased the UDP `RETURN` counter in `EDGE_LAN_DNS_PREROUTING` from `0` to `1`.
- A Fedora LAN query addressed to an external classic DNS server also returned `NOERROR` and increased the UDP `REDIRECT` counter from `0` to `1`. This supports the configured LAN UDP/53 redirect for the tested client. The `dig` `SERVER` line displayed the requested destination and does not independently identify the resolver that answered after redirect.
- The operator reported that Internet access worked on the Android phone using the ASUS as a Tailscale exit node over cellular data. The conversation did not record the public-IP comparison, a client-side packet trace, or an isolated test of management access from another tailnet role. This observation is recorded as a functional smoke test, not a full security-matrix validation.

## State and next validation

`v2.1.4-dev` was deployed with the `de1cf10` source staged on the router. The pre-change off-router archive and the two installer snapshots provide the recorded recovery points. Static checks, live router health, LAN classic-DNS UDP redirection, and operator-reported cellular exit-node browsing passed within their stated scopes.

Before expanding the deployment claims, repeat the role-specific remote-client access tests and capture sanitized evidence if it is needed for publication.

A read-only check of the [GNUton release list](https://github.com/gnuton/asuswrt-merlin.ng/releases/tag/3004.388.11_1-gnuton1) identified `3004.388.11_1-gnuton1` as the latest stable candidate for the TUF-AX5400 line. The release offers separate standard and TUF-interface images. The exact hardware revision and intended interface variant still need confirmation before an image is selected. No firmware image was flashed during this session; later storage and telemetry features were likewise untouched.
